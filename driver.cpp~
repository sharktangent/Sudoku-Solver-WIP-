// driver.cpp
#include <iostream>
#include <cstdlib>
#include "SudokuSolver.h"

int main(int argc, char* argv[]) {
	auto app = Gtk::Application::create(argc, argv);
	
	SudokuSolver solver;
	return app->run(solver);
}
